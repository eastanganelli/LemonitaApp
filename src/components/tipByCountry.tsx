import React from 'react';
import { IonContent, IonLabel } from '@ionic/react';

const TipCalculator: React.FC = () => {
	return (
		<IonContent >
            <IonLabel>{`Future Update`}</IonLabel>
		</IonContent>
	);
};

export default TipCalculator;