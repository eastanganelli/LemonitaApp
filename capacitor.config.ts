import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pesta.TipsCalculatorByBattousai',
  appName: 'TipCalculator',
  webDir: 'dist'
};

export default config;
